@{
    ViewBag.Title = "Welcome";
}

<header>
    <div class="row vertical-align text-center">
        <div class="col-lg-12">
            <h1>Let's get ten random articles from Wikipedia!</h1>
            <h2>@Html.ActionLink("Sure. Let's go!", "GetRandomArticles", "Articles", null, new { @class = "btn btn-success btn-lg btn-fixed-width" })
            @Html.ActionLink("Clear the database!", "ClearDatabase", "Articles", null, new { @class = "btn btn-danger btn-lg btn-fixed-width" })</h2>
        </div>
    </div>
</header>